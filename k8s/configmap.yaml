apiVersion: v1
kind: ConfigMap
metadata:
  name: realestate-config
  namespace: realestate
data:
  PORT: "8080"
  MINIO_ENDPOINT: "minio:9000"
  MINIO_BUCKET: "articles"
  MINIO_USE_SSL: "false"
  CORS_ORIGIN: "https://daugia.vercel.app"
  SECURE_COOKIE: "true"
